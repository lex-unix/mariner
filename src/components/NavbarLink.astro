---
interface Props {
  href: string
}

const { href } = Astro.props
const isActive = href === Astro.url.pathname
---

<a
  href={href}
  class={`hidden ${isActive ? 'opacity-100' : 'opacity-60'} transition-opacity hover:opacity-100 hover:underline md:inline`}
>
  <slot />
</a>
